FastAPI - запустите через docker-compose up -d


 Скопируйте файл .env.example в .env
```bash
cp .env.example .env - скопируйте шаблон и введите свой логин пароль и ключ
Copy-Item .env.example .env

docker-compose up --build



python -m venv venv - создайте виртуальное окружение
venv\Scripts\activate - активируйте виртуальное окружение

Установите библиотеки
pip install -r requirements.txt

Возможно понадобиться отдельно установить
pip install greenlet --only-binary :all:
python -m pip install --upgrade pip
после выполните
pip install -r requirements.txt


docker compose up -d
docker compose exec web alembic upgrade head



установить PostgreSQL




Запуск сервера
uvicorn app.main:app

